name: CI/CD Pipeline for .NET Console App

on:
  push:
    branches:
      - master

permissions:
  contents: write  # Required for creating releases

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]  # Add more OSes if needed (e.g., macos-latest)
        include:
          - os: windows-latest
            runtime: win-x64
          - os: ubuntu-latest
            runtime: linux-x64
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.x'

    - name: Cache .NET dependencies
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Test
      run: dotnet test --no-build --verbosity normal --logger "trx;LogFileName=TestResults/test-results.trx"

    - name: Publish Test Results
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.os }}
        path: TestResults/
      if: always()

    - name: Publish Standalone Executable
      run: dotnet publish --configuration Release -r ${{ matrix.runtime }} --self-contained -o ./publish /p:PublishTrimmed=true /p:TrimMode=link

    - name: List Publish Directory (Debugging)
      run: |
        echo "Contents of ./publish:"
        ls -R ./publish  # For Linux
        # dir ./publish /s  # For Windows (uncomment if needed)

    - name: Ensure Execute Permissions (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        chmod +x ./publish/cstut1

    - name: Clean Up Unnecessary Files (Windows)
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        # Remove unnecessary files (e.g., .pdb, .xml)
        Remove-Item -Recurse -Force ./publish/*.pdb
        Remove-Item -Recurse -Force ./publish/*.xml
        # Ensure only the executable and its dependencies remain
        Get-ChildItem -Path ./publish -Exclude "*.exe", "*.dll", "*.so", "*.dylib", "*.json", "*.config" | Remove-Item -Recurse -Force

    - name: Clean Up Unnecessary Files (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        # Remove unnecessary files (e.g., .pdb, .xml)
        find ./publish -name "*.pdb" -delete
        find ./publish -name "*.xml" -delete
        # Ensure only the executable and its dependencies remain
        find ./publish -type f ! -name "*.exe" ! -name "*.dll" ! -name "*.so" ! -name "*.dylib" ! -name "*.json" ! -name "*.config" -delete

    - name: Install UPX (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y upx

    - name: Compress Executable with UPX (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        # Use the absolute path to the executable
        if [ -f "$GITHUB_WORKSPACE/publish/cstut1" ]; then
          upx --best "$GITHUB_WORKSPACE/publish/cstut1"
        else
          echo "Error: Executable '$GITHUB_WORKSPACE/publish/cstut1' not found!"
          exit 1
        fi

    - name: Install UPX (Windows)
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        # Download UPX for Windows
        Invoke-WebRequest -Uri "https://github.com/upx/upx/releases/download/v4.0.2/upx-4.0.2-win64.zip" -OutFile upx.zip
        Expand-Archive -Path upx.zip -DestinationPath upx
        # Add UPX to PATH
        echo "$env:GITHUB_WORKSPACE\upx\upx-4.0.2-win64" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

    - name: Compress Executable with UPX (Windows)
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        # Use the absolute path to the executable
        if (Test-Path -Path "$env:GITHUB_WORKSPACE\publish\cstut1.exe") {
          upx --best "$env:GITHUB_WORKSPACE\publish\cstut1.exe"
        } else {
          Write-Host "Error: Executable '$env:GITHUB_WORKSPACE\publish\cstut1.exe' not found!"
          exit 1
        }

    - name: Compress Artifacts (Windows)
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        Compress-Archive -Path ./publish/* -DestinationPath cstut1-windows.zip

    - name: Compress Artifacts (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        tar -czvf cstut1-linux.tar.gz -C ./publish .

    - name: Clean Up (Optional)
      shell: pwsh  # Use PowerShell Core
      run: |
        if ("${{ matrix.os }}" -eq "windows-latest") {
          Remove-Item -Recurse -Force ./publish
        } else {
          rm -rf ./publish
        }

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: cstut1-${{ matrix.os }}
        path: |
          cstut1-windows.zip
          cstut1-linux.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download Artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: cstut1-v${{ github.run_number }}
        name: cstut1 Release v${{ github.run_number }}
        draft: false
        prerelease: false
        files: |
          ./artifacts/cstut1-windows-latest/cstut1-windows.zip
          ./artifacts/cstut1-ubuntu-latest/cstut1-linux.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}